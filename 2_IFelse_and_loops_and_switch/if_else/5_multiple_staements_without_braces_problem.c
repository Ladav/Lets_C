#include<stdio.h>
#include<conio.h>
main()
{
    if(1)
        printf("this will a error");
        printf("because the else part is no more connect to the if ,or we can say we isolated the else..");
    else
        printf("false");
}

