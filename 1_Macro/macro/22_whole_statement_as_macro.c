#include<stdio.h>

#define hey =
#define yees (x >= 10)
#define hell int z=0;

main()
{
    int x hey 30;
    hell
    if(yees)
     printf("i replaced '=' with 'hey' and the value of x is %d \n and i had created a condition (x>=10 ||x==10) then this statement will be printed. %d",x,z);
}
