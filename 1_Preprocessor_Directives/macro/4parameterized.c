#include<stdio.h>
#include<conio.h>

#define p(x) printf("the no. is : %d\n",x);

void main()
{
    int tan;
    int cos;
    int a[10];

    p(cos);
    p(tan);
    p(a[6]);
}

//garbage values will be printed
